{
  "name": "todo-server",
  "version": "0.0.1",
  "description": "Simple ToDo API",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "nodemon src/app.js",
    "migrate": "sequelize db:migrate",
    "undo": "sequelize db:migrate:undo",
    "undo:all": "sequelize db:migrate:undo:all"
  },
  "author": "Edward Vincent L. Cuevas",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2",
    "pg": "^8.7.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.14.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "dotenv": "^14.2.0"
  }
}
